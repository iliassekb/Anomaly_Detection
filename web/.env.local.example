# URL of the FastAPI backend (api_server.py)
# For local dev: leave as-is
# For production: set to your deployed backend URL (HuggingFace Spaces, Railway, Render, etc.)
NEXT_PUBLIC_API_URL=http://localhost:8000
